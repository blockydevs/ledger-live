---
---

Refresh coin-modules documentation for the externalized `@ledgerhq/coin-module-framework`: rename `AlpacaApi` to `CoinModuleApi`, document the restricted `@ledgerhq/*` imports, and replace the removed `alpacaized` map with `genericCoinFrameworkFamilies`.
