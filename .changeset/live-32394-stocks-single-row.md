---
"ledger-live-desktop": patch
---

Fix: the Portfolio Stocks "Explore" section now keeps its pills tightly packed at their natural size on two rows, and collapses everything onto a single horizontally scrollable line as soon as the window is wide enough that gaps would otherwise appear between the pills, instead of staying packed to the left on several rows. The global search overlay keeps its compact two-row carousel.
