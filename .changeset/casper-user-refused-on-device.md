---
"@ledgerhq/live-common": patch
---

Map the Casper device rejection return code to `UserRefusedOnDevice`, so a transaction or address rejected on device shows the "Action rejected" copy instead of a raw device error.
